import { createMuiTheme } from '@material-ui/core/styles'


const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#d7cec7',
            dark: '#565656'
        },
        secondary: {
            main: '#c09f80',
            dark: '#76323f'
        }
    }
})

export default theme;